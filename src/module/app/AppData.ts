import { BasePo } from '../../core/bean/BasePo';

export class AppData extends BasePo {
  studentCount: number;
  articleCount: number;
  appointmentCount: number;
  teacherCount: number;
  messageBoardCount: number;
}
